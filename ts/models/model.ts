module ts {
  export module models {

    /**
     * Base class for all API model classes. Just a placeholder for
     * now, for use in instanceof checks.
     */
    export class Model {
      constructor(data?: any) {
      }
    }
  }
}
