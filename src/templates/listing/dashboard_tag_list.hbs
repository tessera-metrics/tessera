<ul class="nav nav-pills nav-stacked">
  <li class="{{#unless tag}}active{{/unless}}">
    <a href="/dashboards">
      All <span class="badge badge-primary pull-right"></span>
    </a>
  </li>

    {{#each tags}}
  <li data-ds-tag="{{name}}">
    <a href="/dashboards/tagged/{{name}}">
      {{name}}
      <span class="badge badge-primary pull-right">{{count}}</span>
    </a>
  </li>
  {{/each}}
</ul>
