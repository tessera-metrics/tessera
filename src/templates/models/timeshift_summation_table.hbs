<div class="ds-item" id="{{item.item_id}}">
  {{> ds-title-bar}}
  <table class="table table-condensed ds-timeshift-summation-table {{height item}} {{css_class item}}
                {{#if item.striped}}table-striped{{/if}}">
    <thead>
      <tr>
        <th></th>
        <th>Now</th>
        <th>{{item.shift}} Ago</th>
        <th>Delta</th>
        <th>%</th>
      </tr>
    </thead>
    <tbody/>
  </table>
</div>
