  <div class="ds-presentation ds-singlestat {{item.css_class}} {{height item}}" id="{{item.item_id}}">
    <h3>{{item.title}}</h3>
    <p><span class="value"></span><span class="unit">{{item.units}}</span></p>
  </div>
