<div class="ds-item ds-graph ds-donut-chart {{css_class item}}"
  id="{{item.item_id}}">
  {{> ds-title-bar}}
  <div class="ds-graph-holder {{height item}}">
    <svg class="{{height item}}"></svg>
  </div>
</div>
